# .dotfiles
For better result use [Catppuccin](https://github.com/catppuccin/catppuccin) theme on iTerm2 Terminal
> PS: this wont work if you already have the dotfiles in your home directory.

## Install
1. Clone this repository
2. Run `install.sh`
3. Open up new window to initiate `zsh` shell

